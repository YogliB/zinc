{
	// Extends root with Node.js-specific options
	// Core package runs in Node.js runtime
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		// Node.js runtime libraries
		"lib": ["ESNext"],
		"target": "ESNext",
		"module": "Preserve",

		// Node.js runtime types
		"types": ["node"],

		// Package-specific path aliases
		"baseUrl": ".",
		"paths": {
			"@/*": ["src/*"]
		}
	},

	// Core package source files
	"include": ["src"],

	// Core package build artifacts
	"exclude": ["node_modules", "dist"]
}
