# Post-Task Commands

Since MVP focuses on functionality without tests/linting/CI:
- For Rust: `cargo check`, `cargo build`
- For Node/Tauri: `npm run tauri dev` or similar once set up.
- No linting or testing enforced yet.